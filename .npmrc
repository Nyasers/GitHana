# 扁平（hoisted）node_modules：不产生符号链接。
# 官方包工具 extension-pack 的源码遍历遇到任何 symlink 会直接拒收
# （"zip source cannot contain symlinks"），且它不排除任何目录——所以这里用
# hoisted 布局，让 App 源码目录可以直接打包。
node-linker=hoisted

# esbuild 的 postinstall 只做自检，不需要它；关掉可避免 ERR_PNPM_IGNORED_BUILDS 让整条链退出 1。
ignore-scripts=true

